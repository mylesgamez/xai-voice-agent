# XAI API Configuration (Required)
XAI_API_KEY=your-xai-api-key-here

# API Endpoint (Optional - defaults to wss://api.x.ai/v1/realtime)
# API_URL=wss://api.x.ai/v1/realtime

# Server Configuration (Required for Twilio)
HOSTNAME=your-ngrok-domain.ngrok-free.app

# Optional: Server Port (defaults to 3000)
# PORT=3000

# X API Configuration (Required for AI Newscaster)
# Get your Bearer Token from https://developer.x.com/en/portal/dashboard
X_BEARER_TOKEN=your-x-bearer-token-here

# Django Backend URL (Required for user OAuth authentication)
# This is where the ai-newscaster Django backend is running
BACKEND_URL=http://localhost:8000
